info:
  name: Revert
  type: http
  seq: 8

http:
  method: POST
  url: "{{BASE_URL}}/api/hestia/recipes/{{HESTIA_RECIPE_ID}}/revert"
  params:
    - name: ""
      value: ""
      type: query
  body:
    type: json
    data: |-
      {
        "targetVersion": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
