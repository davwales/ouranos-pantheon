info:
  name: History
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/api/hestia/recipes/{{HESTIA_RECIPE_ID}}/history"
  params:
    - name: ""
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
