info:
  name: Get
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/api/hestia/shopping-list"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5