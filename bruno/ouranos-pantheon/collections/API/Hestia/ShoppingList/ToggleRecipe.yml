info:
  name: ToggleRecipe
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/api/hestia/shopping-list/recipes/{{HESTIA_RECIPE_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5