info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASE_URL}}/api/hermes/models"
  params:
    - name: ""
      value: ""
      type: query
  body:
    type: json
    data: |-
      {
        "name": "Bruno Test Model",
        "modelIdentifier": "{{MODEL_IDENTIFIER}}",
        "systemPrompt": "You are a helpful assistant.",
        "temperature": 0.7,
        "maxTokens": 1024,
        "repeatPenalty": 1.1,
        "contextWindow": 4096,
        "isDefault": false,
        "isPublic": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
