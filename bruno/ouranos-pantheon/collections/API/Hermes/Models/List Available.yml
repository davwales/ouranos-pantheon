info:
  name: List Available
  type: http
  seq: 6

http:
  method: GET
  url: "{{BASE_URL}}/api/hermes/available-models"
  params:
    - name: ""
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
