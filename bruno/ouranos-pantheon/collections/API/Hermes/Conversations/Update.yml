info:
  name: Update
  type: http
  seq: 4

http:
  method: PUT
  url: "{{BASE_URL}}/api/hermes/conversations/{{CONVERSATION_ID}}"
  params:
    - name: ""
      value: ""
      type: query
  body:
    type: json
    data: |-
      {
        "name": "Bruno chat",
        "personaId": "{{PERSONA_ID}}",
        "modelConfigId": "{{MODEL_ID}}",
        "traitIds": [
          "{{TRAIT_ID}}"
        ],
        "isPublic": true,
        "folderId": null,
        "messages": [
          {
            "role": "User",
            "content": "Hello from Bruno"
          }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
