info:
  name: Get
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/hermes/traits/{{TRAIT_ID}}"
  params:
    - name: ""
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
