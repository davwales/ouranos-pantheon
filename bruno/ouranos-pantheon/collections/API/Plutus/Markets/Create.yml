info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASE_URL}}/api/plutus/markets"
  params:
    - name: ""
      value: ""
      type: query
  body:
    type: json
    data: |-
      {
        "name": "Bruno Demo Market",
        "description": "Market created from Bruno",
        "icon": null,
        "isForecastingEnabled": false,
        "taxes": {
          "flat": {
            "minimum": 0,
            "maximum": 900,
            "rate": 0.3
          }
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
