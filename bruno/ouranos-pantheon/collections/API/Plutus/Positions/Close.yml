info:
  name: Close
  type: http
  seq: 5

http:
  method: POST
  url: "{{BASE_URL}}/api/plutus/positions/{{POSITION_ID}}/close"
  params:
    - name: ""
      value: ""
      type: query
  body:
    type: json
    data: |-
      {
        "closeStatus": "Sold"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
