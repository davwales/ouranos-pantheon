info:
  name: Link
  type: http
  seq: 6

http:
  method: POST
  url: "{{BASE_URL}}/api/plutus/positions/{{POSITION_ID}}/link"
  params:
    - name: ""
      value: ""
      type: query
  body:
    type: json
    data: |-
      {
        "targetPositionId": "{{TARGET_POSITION_ID}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
