info:
  name: Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{BASE_URL}}/api/plutus/strategies/{{STRATEGY_ID}}"
  params:
    - name: ""
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
