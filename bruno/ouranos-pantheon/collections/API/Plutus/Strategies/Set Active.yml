info:
  name: Set Active
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{BASE_URL}}/api/plutus/strategies/{{STRATEGY_ID}}/active"
  params:
    - name: ""
      value: ""
      type: query
  body:
    type: json
    data: |-
      {
        "isActive": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
