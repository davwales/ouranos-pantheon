info:
  name: Market Overview
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/api/plutus/markets/{{MARKET_ID}}/overview?timeFrame=OneHour"
  params:
    - name: timeFrame
      value: "OneHour"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
