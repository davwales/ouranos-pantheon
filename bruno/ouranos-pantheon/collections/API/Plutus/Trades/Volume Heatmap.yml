info:
  name: Volume Heatmap
  type: http
  seq: 5

http:
  method: GET
  url: "{{BASE_URL}}/api/plutus/markets/{{MARKET_ID}}/volume-heatmap?lookbackWeeks=4"
  params:
    - name: lookbackWeeks
      value: "4"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
