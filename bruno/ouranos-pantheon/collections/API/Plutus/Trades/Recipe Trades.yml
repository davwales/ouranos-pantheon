info:
  name: Recipe Trades
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/api/plutus/markets/{{MARKET_ID}}/recipe-trades?timeFrame=OneHour"
  params:
    - name: timeFrame
      value: "OneHour"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
