info:
  name: Market Trades
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/plutus/markets/{{MARKET_ID}}/trades?timeFrame=OneHour"
  params:
    - name: timeFrame
      value: "OneHour"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
