info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASE_URL}}/api/plutus/recipes"
  params:
    - name: ""
      value: ""
      type: query
  body:
    type: json
    data: |-
      {
        "marketId": "{{MARKET_ID}}",
        "name": "Bruno Bronze Bar",
        "cost": 12.5,
        "inputs": [
          {
            "symbolId": "{{SYMBOL_ID}}",
            "name": "Copper ore",
            "quantity": 2
          }
        ],
        "outputs": [
          {
            "symbolId": "{{SYMBOL_ID}}",
            "name": "Bronze bar",
            "quantity": 1
          }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
