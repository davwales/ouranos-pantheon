info:
  name: Get
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/plutus/symbol-groups/{{SYMBOL_GROUP_ID}}?timeFrame=OneDay"
  params:
    - name: timeFrame
      value: "OneDay"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
