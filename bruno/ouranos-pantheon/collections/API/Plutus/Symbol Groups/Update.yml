info:
  name: Update
  type: http
  seq: 4

http:
  method: PUT
  url: "{{BASE_URL}}/api/plutus/symbol-groups/{{SYMBOL_GROUP_ID}}"
  params:
    - name: ""
      value: ""
      type: query
  body:
    type: json
    data: |-
      {
        "symbolGroupId": "{{SYMBOL_GROUP_ID}}",
        "name": "Bruno Basket",
        "description": "Symbol group updated from Bruno",
        "symbolIds": [
          "{{SYMBOL_ID}}"
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
