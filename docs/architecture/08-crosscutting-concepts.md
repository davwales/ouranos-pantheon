# 8. Crosscutting Concepts

Concepts that apply across all modules. Most are implemented in the shared kernel,
`Ouranos.Pantheon.Modules.Shared.Contract` (see [Section 5.6](05-building-block-view.md#56-blackbox-shared)).

## 8.1 Domain Primitives

| Abstraction | Purpose |
|-------------|---------|
| `Id<T>` | Strongly-typed record struct wrapping a string (`Id<Market>` ≠ `Id<Symbol>`; a GUID by convention, generated by the `DatabaseExtensions.CreateId<T>()` factory), with JSON converter |
| `BaseEntity<TId>` | `Id`, `CreatedAt`, `UpdatedAt`, protected `Update()` touch |
| `BaseEventSourcedEntity` | Marten aggregate base with Guid stream key |
| `IDomainEvent` | Marker for domain events |
| `OperationResult` | Explicit success/failure instead of exceptions for expected outcomes |

## 8.2 Application Patterns

- **Handler markers**: `IPantheonHandler<TInput, TOutput>` (query/command),
  `IPantheonHandler<TInput>` (fire-and-forget), `IPantheonStreamHandler<TInput, TOutput>`
  (streaming). Wolverine discovers implementations by scanning module assemblies for
  these markers; there is no mediator registration to maintain.
- **Endpoint dispatch**: every endpoint's `Handle` calls `IMessageBus.InvokeAsync<T>`; the
  bus is the only path between slices.
- **Common query contract**: paging (`skip/take`), sorting, and the
  `field:op:value` filter language, parsed once in the kernel and reused by every list
  endpoint.
- **Step pipeline**: `IStep`/`StepRegistry` abstractions compose the backtest execution
  pipeline via the chain of responsibility pattern
- **Shared Algorithms**: some generic algorithms (`Algorithms/`) have been implemented in the shared kernel for use in various features (e.g., the genetic algorithms used in strategy optimization)

## 8.3 Persistence

- **Schema-per-module**: each `OuranosDbContext` subclass declares its Postgres schema
  (`plutus`, `hermes`, …); EFCore.NamingConventions enforces snake_case.
- **Two persistence styles, one engine:**
  - *Relational + time-series* (Plutus, Hermes, Shared): EF Core; trade, signal, and
    forecast tables are TimescaleDB hypertables with continuous aggregates for market
    views; migrations per module, applied in module `Configure`.
  - *Event sourcing* (Hestia): Marten streams per recipe with inline snapshots.
- **`PostgresModule`**: `AddCorePostgresModule<TContext>()` / `ApplyCorePostgresMigrations<TContext>()`
  provide the shared DAL registration/migration path so modules stay consistent.
- TickerQ's operational store is a separate context, migrated centrally in `UseOuranosCore`.

## 8.4 Messaging (Wolverine + RabbitMQ)

Conventional local routing is disabled; every published message declares its topology as
constants on the message type. `AutoProvision` creates exchanges/queues at startup
(`AutoCreate.CreateOrUpdate` in production).

| Message | Exchange | Queue | DLQ |
|---------|----------|-------|-----|
| `TradeMessage` | `plutus.trade` | `plutus.trade.ingest` | `plutus.trade.ingest.dlq` |
| `RunBacktestMessage` | `plutus.backtest` | `plutus.backtest.run` | `plutus.backtest.run.dlq` |
| `OptimizeStrategyMessage` | `plutus.backtest` | `plutus.backtest.optimize` | `plutus.backtest.optimize.dlq` |
| `ImportRecipeRequested` | `hestia.recipe` | `hestia.recipe.import` | `hestia.recipe.import.dlq` |

**Error policy** (centralized in `CoreExtensions`, registered only when `RabbitMq:RetryCount`
is set): `ArgumentException` and
`InvalidOperationException` move straight to the error queue (they indicate bugs/bad
input, not transient faults); any other exception retries with fixed cooldown intervals
(configured via `RabbitMq:RetryCount`, 250 ms steps) before dead-lettering.

All messages are module-internal async flows; no message crosses module boundaries.

## 8.5 Scheduling (TickerQ)

In-process scheduler with EF Core store and dashboard at `/tickerq/dashboard`; the job
inventory lives in [Section 6.1](06-runtime-view.md#61-scheduled-jobs-overview). Long-running
jobs guard against overlapping ticks (e.g. `SymbolSignalCalculateJob` uses an `Interlocked`
flag).

## 8.6 Errors and Validation

- No FluentValidation. Guards are `Ardalis.GuardClauses` in constructors/handlers
  (`Guard.Against.Null`, `Guard.Against.NotFound`, …) plus domain invariants.
- `services.AddProblemDetails()` provides the RFC 7807 surface; there is no custom
  exception-mapping middleware (noted in [Section 11](11-risks-and-technical-debt.md)).
- Expected outcomes that a client can act on return `OperationResult` (see 8.1) instead of
  throwing; guards and problem details cover the exceptional path.
- The messaging error policy above handles async failure classification.

## 8.7 Logging

Serilog with configuration-driven setup; `UseSerilogRequestLogging` for HTTP. Convention:
`LogTrace` at handler entry, `LogDebug` on success, structured message templates with
named placeholders (`'{paramName}'`). Production ships to Loki; Serilog SelfLog echoes
configuration problems to stderr.

## 8.8 API Conventions

- camelCase JSON with `JsonStringEnumConverter` and `IdJsonConverterFactory` for `Id<T>`
- Async operations return **202 Accepted** with a resource id to poll (backtests,
  optimization, recipe import)
- Query binding is case-insensitive to input record property names
- Bruno collection in
  `bruno/` documents every endpoint's concrete shape

## 8.9 Feature Flags and Configuration

Flagsmith is registered centrally (`AddCoreFlagsmithModule`) and consumed where behavior
toggles matter (currently Hermes handlers via `IFlagsmithClient`). Configuration binds
through typed option classes per section (e.g. `RabbitMqOptions`, `PostgresOptions`,
`QueryOptions`, `HealthOptions`). Caching is `IMemoryCache` only, registered centrally in
`AddOuranosCore`.

## 8.10 Real-Time Infrastructure

The kernel's `WebSockets/` package generalizes long-lived connections: listener registry,
reconnecting client, serializers, and the `WebSocketWorker` hosted service. Plutus hosts
it twice (FFXIV, Stocks) via factory methods in `PlutusModule.Build`; connection state is
surfaced to health checks through `WebSocketHealthState`.

## 8.11 Health Checks

Registered in `AddOuranosCore`: Postgres, RabbitMQ, OuranosMl, WebSocket state, and TickerQ,
aggregated by the Shared module's health endpoint.

## 8.12 Security

There is no authentication or authorization anywhere in `src/`. This is by design for a
single-operator homelab deployment; see
[ADR 0008](../adr/0008-no-authentication-single-user.md) and risk D2 in
[Section 11](11-risks-and-technical-debt.md). The only network controls are the CORS
allow-list (`CorsAllowedHosts`, policy `AllowLocalAndServer`, see 7.3) and the anti-SSRF
guard on the recipe scraper. Anything that can reach the gateway port has full access.
