import IconProps from '@/app/components/core/icons/icon_props';
import MuiReplayIcon from '@mui/icons-material/Replay';

export default function ReplayIcon(props: IconProps) {
    return (
        <MuiReplayIcon {...props} />
    );
}